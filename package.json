{
  "version": "1.0.0",
  "description": "Normalize javascript code to remove randomness introduced by bundling",
  "main": "dist/index.js",
  "type": "module",
  "private": true,
  "workspaces": [
    "lib",
    "site"
  ],
  "scripts": {
    "build": "cd lib && bun run build",
    "site": "cd site && bun run dev"
  }
}